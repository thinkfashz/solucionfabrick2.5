'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { transformHtmlToPremiumPage } from './premiumHtmlTransformer';
import { buildCommercialBlocksFromJson } from './premiumCommercialJsonBlocks';

type Device = 'phone' | 'tablet' | 'desktop' | 'wide';
type VisualPreset = 'fabrick-lava' | 'glass-rose' | 'luxury-soft' | 'mobile-app-premium' | 'booking-beauty' | 'neo-minimal' | 'editorial-dark';
type BlockType = 'hero' | 'cards' | 'split' | 'cta' | 'calculator' | 'custom';
type JsonState = 'idle' | 'valid' | 'invalid' | 'applied';
type Block = { id: string; type: BlockType; title: string; text: string; background: string; textColor: string; accent: string; html?: string; image?: string; buttonText?: string; buttonHref?: string };
type PageState = { title: string; token?: string; device: Device; visualPreset: VisualPreset; blocks: Block[] };
type PageDoc = { token: string; title: string; status: string; updated_at?: string | null; expires_at?: string | null };

const STORAGE = 'sf_page_engine_21stdev_v8';
const STORAGE_UPDATED_AT = `${STORAGE}_updated_at`;
const STORAGE_PUBLISHED_AT = `${STORAGE}_published_at`;
const TYPES: BlockType[] = ['hero', 'cards', 'split', 'calculator', 'cta', 'custom'];
const DEVICES: Device[] = ['phone', 'tablet', 'desktop', 'wide'];
const PRESETS: Record<VisualPreset, { label: string; description: string; background: string; surface: string; text: string; accent: string; darkText: string }> = {
  'fabrick-lava': { label: 'Fabrick Lava', description: 'Negro, amarillo y naranja comercial premium.', background: '#0d0906', surface: '#fff7e9', text: '#fff7e8', accent: '#f59e0b', darkText: '#17120a' },
  'glass-rose': { label: 'Glass Rose', description: 'Cristal rosado, suave y aspiracional.', background: '#1a1117', surface: '#fff1f7', text: '#fff7fb', accent: '#f48fb1', darkText: '#241219' },
  'luxury-soft': { label: 'Luxury Soft', description: 'Ivory, beige y dorado suave.', background: '#1b1510', surface: '#f8f1e8', text: '#fff8ef', accent: '#d6a85f', darkText: '#1e1409' },
  'mobile-app-premium': { label: 'Mobile App Premium', description: 'Azul profundo estilo app premium.', background: '#0d2443', surface: '#eff5ff', text: '#f5f9ff', accent: '#6db4ff', darkText: '#091726' },
  'booking-beauty': { label: 'Booking Beauty', description: 'Beauty booking, rosado limpio y elegante.', background: '#28171d', surface: '#fff4f7', text: '#fff8fb', accent: '#ff7db2', darkText: '#29141c' },
  'neo-minimal': { label: 'Neo Minimal', description: 'Minimalismo limpio con grises suaves.', background: '#111214', surface: '#f6f7f8', text: '#ffffff', accent: '#cfd4dc', darkText: '#151719' },
  'editorial-dark': { label: 'Editorial Dark', description: 'Oscuro, tipografía fuerte y editorial.', background: '#070707', surface: '#f4f1eb', text: '#f7f4ef', accent: '#c9a46a', darkText: '#17120a' },
};
const PRESET_LIST = Object.keys(PRESETS) as VisualPreset[];

function uid() { return typeof crypto !== 'undefined' && 'randomUUID' in crypto ? crypto.randomUUID() : `${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`; }
function esc(v: unknown) { return String(v ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;'); }
function text(v: unknown, fallback = '') { const s = typeof v === 'string' ? v.trim() : typeof v === 'number' ? String(v) : ''; return s || fallback; }
function color(v: unknown, fallback: string) { const s = text(v); return /^#[0-9a-fA-F]{3,8}$/.test(s) ? s : fallback; }
function obj(v: unknown): Record<string, unknown> { return v && typeof v === 'object' && !Array.isArray(v) ? v as Record<string, unknown> : {}; }
function presetType(v: unknown): VisualPreset { const p = text(v, 'fabrick-lava') as VisualPreset; return PRESET_LIST.includes(p) ? p : 'fabrick-lava'; }
function blockType(v: unknown): BlockType { const t = text(v, 'custom') as BlockType; return TYPES.includes(t) ? t : 'custom'; }
function safeHtml(v: unknown) { return text(v).replace(/<script\b[^>]*>[\s\S]*?<\/script>/gi, '').replace(/<iframe\b[^>]*>[\s\S]*?<\/iframe>/gi, '').replace(/<object\b[^>]*>[\s\S]*?<\/object>/gi, '').replace(/<embed\b[^>]*>/gi, '').replace(/\son[a-z]+\s*=\s*"[^"]*"/gi, '').replace(/\son[a-z]+\s*=\s*'[^']*'/gi, '').replace(/javascript:/gi, ''); }
function localItem(key: string) { if (typeof window === 'undefined') return ''; try { return window.localStorage.getItem(key) || ''; } catch { return ''; } }
function timeLabel(iso: string) { if (!iso) return 'sin registro'; try { return new Date(iso).toLocaleString('es-CL', { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit' }); } catch { return iso; } }
function joinItems(items: unknown[]) { return items.map((item) => typeof item === 'string' ? item : text(obj(item).title ?? obj(item).name ?? obj(item).label ?? obj(item).text ?? obj(item).description)).filter(Boolean).join(' | '); }
function firstObj(source: Record<string, unknown>, keys: string[]) { for (const key of keys) { const value = obj(source[key]); if (Object.keys(value).length) return value; } return {}; }
function firstArray(source: Record<string, unknown>, keys: string[]) { for (const key of keys) if (Array.isArray(source[key]) && (source[key] as unknown[]).length) return source[key] as unknown[]; return []; }

function makeBlock(type: BlockType): Block {
  const base: Block = { id: uid(), type, title: 'Nuevo bloque Fabrick', text: 'Edita este contenido desde el inspector.', background: '#0d0a06', textColor: '#fff7e8', accent: '#f59e0b', buttonText: 'Solicitar presupuesto', buttonHref: '/contacto' };
  if (type === 'cards') return { ...base, title: 'Beneficios', text: 'Diseño rápido | Presupuesto conectado | Seguimiento comercial', background: '#fff7e9', textColor: '#17120a' };
  if (type === 'split') return { ...base, title: 'Convierte visitas en clientes', text: 'Combina página, propuesta y automatización para vender más rápido.', image: 'https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1200&q=80' };
  if (type === 'calculator') return { ...base, title: 'Calculadoras conectadas', text: 'Radier, aire acondicionado y presupuestos automáticos.' };
  if (type === 'cta') return { ...base, title: '¿Listo para cotizar?', text: 'Activa el motor y comparte un link con vencimiento.', background: '#f59e0b', textColor: '#17120a' };
  if (type === 'custom') return { ...base, title: 'HTML personalizado', text: '', background: '#ffffff', textColor: '#111111', html: '<section><h2>Bloque personalizado</h2><p>Edita este HTML.</p></section>' };
  return { ...base, title: 'Landing comercial creada con motor Fabrick', text: 'Presenta tu oferta, conecta presupuestos y comparte una URL privada con vencimiento.' };
}
function applyPreset(page: PageState, preset: VisualPreset): PageState { const meta = PRESETS[preset]; return { ...page, visualPreset: preset, blocks: page.blocks.map((b) => b.type === 'cards' ? { ...b, background: meta.surface, textColor: meta.darkText, accent: meta.accent } : b.type === 'cta' ? { ...b, background: meta.accent, textColor: meta.darkText, accent: meta.accent } : b.type === 'custom' ? { ...b, accent: meta.accent } : { ...b, background: meta.background, textColor: meta.text, accent: meta.accent }) }; }
function normalizeBlock(raw: unknown, index: number, defaults?: Partial<Block>): Block { const s = obj(raw); const type = blockType(s.type ?? s.kind ?? s.component); const f = { ...makeBlock(type), ...defaults }; return { ...f, id: text(s.id, f.id), type, title: text(s.title ?? s.heading ?? s.headline ?? s.name, f.title || `Bloque ${index + 1}`), text: text(s.text ?? s.description ?? s.subtitle ?? s.copy, f.text), background: color(s.background ?? s.bg ?? s.bgColor, f.background), textColor: color(s.textColor ?? s.color, f.textColor), accent: color(s.accent ?? s.primary ?? s.buttonColor, f.accent), html: safeHtml(s.html ?? s.markup ?? f.html), image: text(s.image ?? s.image_url ?? s.cover ?? s.photo, f.image || ''), buttonText: text(s.buttonText ?? s.cta ?? s.ctaText ?? s.button_label, f.buttonText), buttonHref: text(s.buttonHref ?? s.href ?? s.url ?? s.link, f.buttonHref) }; }
function buildBlocks(source: Record<string, unknown>, defaults: Partial<Block>) { const blocks: Block[] = []; const hero = firstObj(source, ['hero', 'header', 'cover', 'main']); if (Object.keys(hero).length || text(source.headline ?? source.title ?? source.description)) blocks.push(normalizeBlock({ type: 'hero', title: hero.title ?? hero.heading ?? hero.headline ?? source.headline ?? source.title, text: hero.text ?? hero.description ?? hero.subtitle ?? source.subtitle ?? source.description, image: hero.image ?? hero.cover ?? source.image, buttonText: hero.buttonText ?? hero.cta ?? source.ctaText, buttonHref: hero.buttonHref ?? hero.href ?? source.ctaHref }, blocks.length, defaults)); const features = firstArray(source, ['features', 'benefits', 'services', 'cards', 'bullets', 'items']); if (features.length) blocks.push(normalizeBlock({ type: 'cards', title: source.featuresTitle ?? source.benefitsTitle ?? 'Beneficios', text: joinItems(features), background: '#fff7e9', textColor: '#17120a' }, blocks.length, defaults)); const split = firstObj(source, ['about', 'problem', 'solution', 'story', 'details']); if (Object.keys(split).length) blocks.push(normalizeBlock({ type: 'split', title: split.title ?? split.heading ?? 'Detalle de la propuesta', text: split.text ?? split.description ?? split.copy, image: split.image ?? split.cover }, blocks.length, defaults)); if (source.calculator === true || source.calculators === true || source.hasCalculator === true) blocks.push(normalizeBlock({ type: 'calculator', title: 'Motores conectados', text: 'Genera presupuestos conectados a tus herramientas internas.' }, blocks.length, defaults)); buildCommercialBlocksFromJson(source).forEach((block) => blocks.push(normalizeBlock(block, blocks.length, defaults))); const cta = firstObj(source, ['cta', 'callToAction', 'finalCta']); if (Object.keys(cta).length || text(source.ctaText ?? source.ctaHref)) blocks.push(normalizeBlock({ type: 'cta', title: cta.title ?? source.ctaTitle ?? '¿Listo para avanzar?', text: cta.text ?? cta.description ?? 'Solicita una propuesta y recibe respuesta rápida.', buttonText: cta.buttonText ?? cta.label ?? source.ctaText ?? 'Contactar', buttonHref: cta.buttonHref ?? cta.href ?? source.ctaHref ?? '/contacto' }, blocks.length, defaults)); if (!blocks.length && text(source.html)) blocks.push(normalizeBlock({ type: 'custom', title: source.title ?? 'HTML importado', html: source.html, background: '#ffffff', textColor: '#111111' }, blocks.length)); return blocks; }
function normalizePage(raw: unknown): PageState { const s = obj(raw); const preset = presetType(s.visualPreset ?? s.preset ?? s.template); const meta = PRESETS[preset]; const theme = { ...obj(s.theme), ...obj(s.colors) }; const defaults = { background: color(theme.background ?? theme.bg, meta.background), textColor: color(theme.textColor ?? theme.text, meta.text), accent: color(theme.accent ?? theme.primary ?? theme.button, meta.accent) }; const rawBlocks = Array.isArray(s.blocks) ? s.blocks : Array.isArray(s.sections) ? s.sections : []; const blocks = rawBlocks.length ? rawBlocks.map((b, i) => normalizeBlock(b, i, defaults)) : buildBlocks(s, defaults); const device = text(s.device, 'desktop') as Device; return applyPreset({ title: text(s.title ?? s.name ?? s.pageTitle ?? s.headline, 'Landing Fabrick'), token: text(s.token, ''), device: DEVICES.includes(device) ? device : 'desktop', visualPreset: preset, blocks: blocks.length ? blocks : [makeBlock('hero'), makeBlock('cards'), makeBlock('cta')] }, preset); }
function parseJson(value: string) { try { const page = normalizePage(JSON.parse(value)); return { ok: true as const, page, message: `JSON válido. ${page.blocks.length} bloque(s), preset ${PRESETS[page.visualPreset].label}.` }; } catch (err) { return { ok: false as const, message: `JSON inválido: ${(err as Error).message}` }; } }
function htmlToPage(raw: string): PageState { return normalizePage(transformHtmlToPremiumPage(raw)); }
function initialState(): PageState { if (typeof window !== 'undefined') { try { const saved = window.localStorage.getItem(STORAGE) || window.localStorage.getItem('sf_page_engine_21stdev_v7'); if (saved) return normalizePage(JSON.parse(saved)); } catch {} } return normalizePage({ title: 'Landing Fabrick', visualPreset: 'fabrick-lava', blocks: [makeBlock('hero'), makeBlock('cards'), makeBlock('split'), makeBlock('calculator'), makeBlock('cta')] }); }
function css() { return `<style>*{box-sizing:border-box}body{margin:0;background:#050302;color:#fff7e8;font-family:Inter,system-ui,sans-serif}a{text-decoration:none;color:inherit}.page-wrap{--page-bg:#100904;--page-text:#fff7e8;--card-bg:linear-gradient(180deg,rgba(255,255,255,.92),rgba(255,247,233,.84));--panel-bg:rgba(255,255,255,.18);--panel-border:rgba(255,190,56,.12);--glow1:rgba(255,192,56,.18);--glow2:rgba(255,96,24,.12);--accent:#f59e0b;--btn-text:#150b04;overflow:hidden;background:radial-gradient(circle at 8% 0%,var(--glow1),transparent 30rem),radial-gradient(circle at 92% 18%,var(--glow2),transparent 26rem),linear-gradient(180deg,var(--page-bg),#050302 70%);color:var(--page-text)}.page-wrap[data-preset="glass-rose"]{--page-bg:#1a1117;--page-text:#fff7fb;--card-bg:linear-gradient(180deg,rgba(255,244,249,.84),rgba(255,234,244,.68));--panel-bg:rgba(255,255,255,.18);--panel-border:rgba(255,192,218,.28);--glow1:rgba(255,150,200,.18);--glow2:rgba(255,210,230,.14);--accent:#f48fb1;--btn-text:#241219}.page-wrap[data-preset="luxury-soft"]{--page-bg:#17120d;--page-text:#fff8ef;--card-bg:linear-gradient(180deg,rgba(249,243,234,.96),rgba(241,233,222,.88));--panel-bg:rgba(255,250,243,.17);--panel-border:rgba(214,168,95,.26);--glow1:rgba(223,190,145,.14);--glow2:rgba(255,244,219,.08);--accent:#d6a85f;--btn-text:#1e1409}.page-wrap[data-preset="mobile-app-premium"]{--page-bg:#0d2443;--page-text:#f5f9ff;--card-bg:linear-gradient(180deg,rgba(242,247,255,.96),rgba(229,239,255,.9));--panel-bg:rgba(255,255,255,.12);--panel-border:rgba(120,176,255,.24);--glow1:rgba(84,147,255,.18);--glow2:rgba(149,204,255,.12);--accent:#6db4ff;--btn-text:#091726}.page-wrap[data-preset="booking-beauty"]{--page-bg:#28171d;--page-text:#fff8fb;--card-bg:linear-gradient(180deg,rgba(255,247,250,.95),rgba(255,236,243,.88));--panel-bg:rgba(255,255,255,.16);--panel-border:rgba(255,161,199,.24);--glow1:rgba(255,134,185,.18);--glow2:rgba(255,214,232,.14);--accent:#ff7db2;--btn-text:#29141c}.page-wrap[data-preset="neo-minimal"]{--page-bg:#111214;--page-text:#fff;--card-bg:linear-gradient(180deg,rgba(248,249,250,.96),rgba(240,242,244,.9));--panel-bg:rgba(255,255,255,.1);--panel-border:rgba(207,212,220,.22);--glow1:rgba(200,205,214,.12);--glow2:rgba(255,255,255,.06);--accent:#cfd4dc;--btn-text:#151719}.page-wrap[data-preset="editorial-dark"]{--page-bg:#070707;--page-text:#f7f4ef;--card-bg:linear-gradient(180deg,rgba(245,242,235,.95),rgba(235,228,215,.9));--panel-bg:rgba(255,255,255,.11);--panel-border:rgba(201,164,106,.2);--glow1:rgba(201,164,106,.11);--glow2:rgba(255,255,255,.05);--accent:#c9a46a;--btn-text:#17120a}.block{position:relative;padding:86px 44px}.inner{position:relative;width:min(1120px,100%);margin:0 auto}.kicker{display:inline-flex;border:1px solid var(--panel-border);background:var(--panel-bg);color:var(--page-text);border-radius:999px;padding:9px 14px;font-size:11px;font-weight:1000;letter-spacing:.22em;text-transform:uppercase}h1,h2{font-size:clamp(42px,7vw,92px);line-height:.9;letter-spacing:-.07em;margin:22px 0 16px}p{font-size:clamp(16px,2vw,20px);line-height:1.7;max-width:820px;color:color-mix(in srgb,currentColor 78%,transparent)}.btn{display:inline-flex;margin-top:30px;border-radius:999px;background:linear-gradient(135deg,#fff8ef 0%,var(--accent) 54%,#fff 130%);color:var(--btn-text);padding:16px 23px;font-weight:1000;box-shadow:0 22px 65px rgba(0,0,0,.18)}.hero{min-height:620px;display:grid;place-items:center;overflow:hidden}.hero-bg{position:absolute;inset:0;background-size:cover;background-position:center;opacity:.36}.hero:after{content:"";position:absolute;inset:18px;border:1px solid var(--panel-border);border-radius:34px}.cards-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px;margin-top:34px}.card{position:relative;overflow:hidden;border-radius:30px;background:var(--card-bg);padding:28px;box-shadow:0 22px 60px rgba(20,10,0,.10)}.card b{display:inline-grid;place-items:center;width:42px;height:42px;border-radius:16px;background:#17120a;color:var(--accent)}.card p{font-size:15px;color:#5f4c2d}.split-grid{display:grid;grid-template-columns:1.04fr .96fr;gap:34px;align-items:center}.split-grid img{width:100%;border-radius:34px;min-height:360px;object-fit:cover}.split-copy,.cta-panel{border:1px solid var(--panel-border);border-radius:34px;background:var(--panel-bg);backdrop-filter:blur(14px);padding:34px}.calc-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin-top:28px}.calc-grid a{border:1px solid var(--panel-border);border-radius:28px;padding:26px;background:var(--panel-bg)}.center{text-align:center}.center p{margin-inline:auto}.cta-panel{width:min(980px,100%);margin:0 auto;padding:54px}.custom-shell{width:min(1120px,100%);margin:0 auto;border-radius:28px;overflow:hidden}.footer-note{padding:26px;text-align:center;color:#8f7651;background:#050302;font-size:12px;letter-spacing:.18em;text-transform:uppercase}@media(max-width:820px){.block{padding:54px 22px!important}.hero{min-height:560px}.cards-grid,.split-grid,.calc-grid{grid-template-columns:1fr}.cta-panel{padding:34px 22px}h1,h2{font-size:42px!important}}</style>`; }
function renderBlock(b: Block, live = false) { const st = `style="background:${esc(b.background)};color:${esc(b.textColor)};--accent:${esc(b.accent)}"`; const attrs = live ? ` data-id="${esc(b.id)}"` : ''; const btn = esc(b.buttonText || 'Solicitar presupuesto'); const href = esc(b.buttonHref || '/contacto'); if (b.type === 'hero') return `<section class="block hero"${attrs} ${st}>${b.image ? `<div class="hero-bg" style="background-image:url('${esc(b.image)}')"></div>` : ''}<div class="inner"><span class="kicker">Soluciones Fabrick</span><h1>${esc(b.title)}</h1><p>${esc(b.text)}</p><a class="btn" href="${href}">${btn} →</a></div></section>`; if (b.type === 'cards') return `<section class="block"${attrs} ${st}><div class="inner"><span class="kicker">Beneficios</span><h2>${esc(b.title)}</h2><div class="cards-grid">${b.text.split('|').map((p, i) => `<article class="card"><b>0${i + 1}</b><h3>${esc(p.trim())}</h3><p>Beneficio preparado para mostrar valor, confianza y claridad comercial.</p></article>`).join('')}</div></div></section>`; if (b.type === 'split') return `<section class="block"${attrs} ${st}><div class="inner split-grid"><div class="split-copy"><span class="kicker">Estrategia</span><h2>${esc(b.title)}</h2><p>${esc(b.text)}</p><a class="btn" href="${href}">${btn} →</a></div>${b.image ? `<img src="${esc(b.image)}" alt="" loading="lazy" />` : ''}</div></section>`; if (b.type === 'calculator') return `<section class="block"${attrs} ${st}><div class="inner"><span class="kicker">Motores conectados</span><h2>${esc(b.title)}</h2><p>${esc(b.text)}</p><div class="calc-grid"><a href="/admin/motores/radier"><strong>Radier 3D</strong><span>Cubicación + presupuesto</span></a><a href="/admin/motores/aire-acondicionado"><strong>Aire acondicionado</strong><span>BTU + instalación</span></a></div></div></section>`; if (b.type === 'cta') return `<section class="block center"${attrs} ${st}><div class="cta-panel"><h2>${esc(b.title)}</h2><p>${esc(b.text)}</p><a class="btn" href="${href}">${btn} →</a></div></section>`; return `<section class="block"${attrs} ${st}><div class="custom-shell">${safeHtml(b.html || '')}</div></section>`; }
function finalHtml(s: PageState) { return `<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(s.title)}</title>${css()}</head><body><main class="page-wrap" data-preset="${esc(s.visualPreset)}">${s.blocks.map((b) => renderBlock(b)).join('')}<div class="footer-note">Generado con Soluciones Fabrick · propuesta privada</div></main></body></html>`; }

export default function FabrickPageEngine21stClient() {
  const start = useMemo(() => initialState(), []); const recovered = useMemo(() => Boolean(localItem(STORAGE) || localItem('sf_page_engine_21stdev_v7')), []); const [state, setState] = useState<PageState>(start); const [selectedId, setSelectedId] = useState(start.blocks[0]?.id || ''); const [expiresHours, setExpiresHours] = useState(168); const [publicUrl, setPublicUrl] = useState(''); const [status, setStatus] = useState(''); const [jsonText, setJsonText] = useState(() => JSON.stringify(start, null, 2)); const [jsonState, setJsonState] = useState<JsonState>('idle'); const [jsonMessage, setJsonMessage] = useState('Pega JSON o sube un archivo para actualizar la vista previa.'); const [saving, setSaving] = useState(false); const [docs, setDocs] = useState<PageDoc[]>([]); const [loadingDocs, setLoadingDocs] = useState(false); const [dirty, setDirty] = useState(false); const [lastAutosave, setLastAutosave] = useState(() => localItem(STORAGE_UPDATED_AT)); const [lastPublishedAt, setLastPublishedAt] = useState(() => localItem(STORAGE_PUBLISHED_AT)); const [showRecovery, setShowRecovery] = useState(recovered); const fileRef = useRef<HTMLInputElement | null>(null); const selected = state.blocks.find((b) => b.id === selectedId) || state.blocks[0]; const html = useMemo(() => finalHtml(state), [state]); const pageWidth = state.device === 'phone' ? 'max-w-[390px]' : state.device === 'tablet' ? 'max-w-[780px]' : state.device === 'wide' ? 'max-w-[1440px]' : 'max-w-[1180px]';
  useEffect(() => { const h = (e: BeforeUnloadEvent) => { if (!dirty) return; e.preventDefault(); e.returnValue = ''; }; window.addEventListener('beforeunload', h); return () => window.removeEventListener('beforeunload', h); }, [dirty]);
  function save(next: PageState, markDirty = true) { const clean = normalizePage(next); const now = new Date().toISOString(); setState(clean); setJsonText(JSON.stringify(clean, null, 2)); setLastAutosave(now); if (markDirty) setDirty(true); try { localStorage.setItem(STORAGE, JSON.stringify(clean)); localStorage.setItem(STORAGE_UPDATED_AT, now); } catch {} }
  function patch(id: string, part: Partial<Block>) { save({ ...state, blocks: state.blocks.map((b) => b.id === id ? { ...b, ...part } : b) }); }
  function add(type: BlockType) { const b = makeBlock(type); save({ ...state, blocks: [...state.blocks, b] }); setSelectedId(b.id); }
  function remove(id: string) { const blocks = state.blocks.filter((b) => b.id !== id); save({ ...state, blocks }); setSelectedId(blocks[0]?.id || ''); }
  function move(id: string, dir: -1 | 1) { const i = state.blocks.findIndex((b) => b.id === id); const t = i + dir; if (i < 0 || t < 0 || t >= state.blocks.length) return; const blocks = [...state.blocks]; [blocks[i], blocks[t]] = [blocks[t], blocks[i]]; save({ ...state, blocks }); }
  function setPreset(preset: VisualPreset) { save(applyPreset({ ...state, visualPreset: preset }, preset)); }
  function validateJson(value = jsonText) { const r = parseJson(value); setJsonState(r.ok ? 'valid' : 'invalid'); setJsonMessage(r.message); return r; }
  function applyJson() { const r = validateJson(jsonText); if (!r.ok) return; save(r.page); setSelectedId(r.page.blocks[0]?.id || ''); setJsonState('applied'); setJsonMessage('JSON aplicado. La vista previa ya está actualizada.'); }
  function pasteJson() { navigator.clipboard?.readText().then((v) => { setJsonText(v); const r = parseJson(v); setJsonState(r.ok ? 'valid' : 'invalid'); setJsonMessage(r.message); if (r.ok) { save(r.page); setSelectedId(r.page.blocks[0]?.id || ''); setJsonState('applied'); } }).catch(() => setJsonMessage('No pude leer el portapapeles.')); }
  async function readFile(file: File) { setStatus(`Leyendo ${file.name}…`); const content = await file.text(); if (file.name.toLowerCase().endsWith('.json')) { setJsonText(content); const r = parseJson(content); setJsonState(r.ok ? 'valid' : 'invalid'); setJsonMessage(r.message); if (r.ok) { save(r.page); setSelectedId(r.page.blocks[0]?.id || ''); setJsonState('applied'); setStatus('Archivo JSON aplicado.'); } else setStatus('El archivo JSON tiene errores.'); return; } const next = htmlToPage(content); save(next); setSelectedId(next.blocks[0]?.id || ''); setJsonState('applied'); setJsonMessage(`HTML transformado a UI premium con preset ${PRESETS[next.visualPreset].label}.`); setStatus('HTML transformado y aplicado como landing premium.'); }
  async function loadDocs(showStatus = true) { setLoadingDocs(true); if (showStatus) setStatus('Cargando páginas guardadas…'); try { const res = await fetch('/api/admin/page-engine', { cache: 'no-store' }); const json = await res.json().catch(() => ({})); if (!res.ok) throw new Error(json.error || `Error ${res.status}`); setDocs(json.documents || []); if (showStatus) setStatus('Páginas guardadas cargadas.'); } catch (err) { setStatus(`Error cargando páginas: ${(err as Error).message}`); } finally { setLoadingDocs(false); } }
  async function publish() { if (!state.title.trim()) return setStatus('Error: título obligatorio.'); if (!state.blocks.length) return setStatus('Error: agrega al menos un bloque.'); setSaving(true); setStatus('Guardando página en BD…'); try { const res = await fetch('/api/admin/page-engine', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token: state.token, title: state.title, html, project_json: state, expires_in_hours: expiresHours, status: 'publicado' }) }); const json = await res.json().catch(() => ({})); if (!res.ok) throw new Error(json.error || `Error ${res.status}`); save({ ...state, token: json.token }, false); const publishedAt = new Date().toISOString(); setDirty(false); setLastPublishedAt(publishedAt); try { localStorage.setItem(STORAGE_PUBLISHED_AT, publishedAt); } catch {} setPublicUrl(json.public_url || ''); setStatus('Página publicada y guardada correctamente.'); await loadDocs(false); } catch (err) { setStatus(`Error: ${(err as Error).message}`); } finally { setSaving(false); } }
  function template() { const next = normalizePage({ title: 'Landing beauty premium', visualPreset: 'booking-beauty', hero: { headline: 'Reserva una experiencia premium', subtitle: 'Diseño aspiracional, agenda rápida y presentación que convierte.', cta: 'Reservar ahora', href: '/contacto' }, benefits: ['Experiencia elegante', 'Conversión más alta', 'Interfaz visual premium'], pricing: [{ name: 'Base', price: '$99.000', features: ['Landing premium', 'Publicación privada'] }, { name: 'Pro', price: '$199.000', features: ['Landing premium', 'Mockup app', 'Soporte'] }], stats: [{ value: '+30%', label: 'más conversión visual' }], testimonials: [{ quote: 'La propuesta se ve mucho más profesional y fácil de vender.', name: 'Cliente Fabrick' }], guarantee: { title: 'Garantía visual y soporte', description: 'Revisión de detalles y acompañamiento posterior.' }, cta: { title: 'Haz que tu marca se vea vendible', text: 'Convierte tu servicio en una experiencia visual de alto valor.', buttonText: 'Solicitar demo', href: '/contacto' } }); save(next); setSelectedId(next.blocks[0]?.id || ''); setJsonState('applied'); setJsonMessage('Plantilla comercial premium aplicada.'); }
  function clearDraft() { try { localStorage.removeItem(STORAGE); localStorage.removeItem('sf_page_engine_21stdev_v7'); localStorage.removeItem(STORAGE_UPDATED_AT); localStorage.removeItem(STORAGE_PUBLISHED_AT); } catch {} setDirty(false); setLastAutosave(''); setLastPublishedAt(''); setShowRecovery(false); setStatus('Borrador local limpiado.'); }
  const exampleJson = `{"title":"Página comercial","visualPreset":"booking-beauty","hero":{"headline":"Reserva una experiencia premium","subtitle":"Diseño aspiracional y presentación que convierte.","cta":"Reservar ahora","href":"/contacto"},"benefits":["Experiencia elegante","Conversión más alta","Interfaz visual premium"],"pricing":[{"name":"Base","price":"$99.000","features":["Landing premium","Link privado"]},{"name":"Pro","price":"$199.000","features":["Mockup app","Soporte","Garantía"]}],"stats":[{"value":"+30%","label":"más conversión visual"}],"testimonials":[{"quote":"Excelente presentación, se ve mucho más profesional.","name":"Cliente"}],"guarantee":{"title":"Garantía y soporte","description":"Revisión de detalles y acompañamiento posterior."},"cta":{"title":"Haz que tu marca se vea vendible","buttonText":"Solicitar demo"}}`;
  return <main className="grid h-screen min-h-[760px] grid-cols-1 overflow-hidden bg-[#070503] text-white lg:grid-cols-[340px_1fr]"><input ref={fileRef} type="file" accept=".json,.html,.htm,application/json,text/html" hidden onChange={(e) => { const f = e.target.files?.[0]; if (f) void readFile(f); e.currentTarget.value = ''; }} /><aside className="min-h-0 overflow-hidden border-r border-yellow-300/15 bg-[#0d0906]/95"><div className="border-b border-yellow-300/15 p-4"><p className="text-[10px] font-black uppercase tracking-[0.28em] text-yellow-300">Fabrick Page Engine</p><h1 className="mt-2 text-xl font-black">HTML / JSON Renderer</h1><p className="mt-1 text-xs text-[#9f8d74]">Editor modular, presets premium y JSON comercial.</p></div><div className="flex gap-2 border-b border-yellow-300/15 p-3">{DEVICES.map((d) => <button key={d} onClick={() => save({ ...state, device: d })} className={`flex-1 rounded-xl px-2 py-2 text-[11px] font-black ${state.device === d ? 'bg-yellow-400 text-black' : 'bg-white/[0.06] text-[#9f8d74]'}`}>{d}</button>)}</div><div className="grid gap-2 border-b border-yellow-300/15 p-3"><label className="grid gap-1 text-[10px] font-black uppercase tracking-[0.22em] text-yellow-300">Preset visual<select value={state.visualPreset} onChange={(e) => setPreset(e.target.value as VisualPreset)} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-3 text-xs text-white outline-none">{PRESET_LIST.map((p) => <option key={p} value={p}>{PRESETS[p].label}</option>)}</select></label><p className="text-xs text-[#9f8d74]">{PRESETS[state.visualPreset].description}</p></div><div className="grid gap-3 border-b border-yellow-300/15 p-3"><input value={state.title} onChange={(e) => save({ ...state, title: e.target.value })} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-3 text-sm outline-none" /><div className="grid grid-cols-2 gap-2"><button onClick={() => fileRef.current?.click()} className="rounded-2xl border border-yellow-300/20 bg-yellow-400/[0.08] px-3 py-3 text-xs font-black text-yellow-100">Subir JSON/HTML</button><button onClick={pasteJson} className="rounded-2xl border border-yellow-300/20 bg-yellow-400/[0.08] px-3 py-3 text-xs font-black text-yellow-100">Pegar JSON</button><button onClick={template} className="rounded-2xl border border-white/10 bg-white/[0.06] px-3 py-3 text-xs font-black">Plantilla comercial</button><button onClick={() => void loadDocs()} className="rounded-2xl border border-white/10 bg-white/[0.06] px-3 py-3 text-xs font-black">{loadingDocs ? 'Cargando…' : 'Guardadas'}</button></div></div><div className="grid gap-2 px-3 py-3">{TYPES.map((type) => <button key={type} onClick={() => add(type)} className="rounded-2xl border border-dashed border-yellow-300/30 bg-yellow-400/[0.07] p-3 text-left"><strong className="text-sm">+ {type}</strong></button>)}</div><div className="min-h-0 overflow-auto border-t border-yellow-300/15 p-3">{state.blocks.map((b, i) => <div key={b.id} className={`mb-2 grid grid-cols-[1fr_auto] gap-2 rounded-2xl border p-3 ${selectedId === b.id ? 'border-yellow-300/70 bg-yellow-400/10' : 'border-white/10 bg-white/[0.04]'}`}><button onClick={() => setSelectedId(b.id)} className="min-w-0 text-left"><strong className="block truncate text-sm">{i + 1}. {b.type}</strong><span className="text-xs text-[#9f8d74]">{b.title}</span></button><div className="flex gap-1"><button onClick={() => move(b.id, -1)} className="rounded-lg bg-white/10 px-2 text-xs">↑</button><button onClick={() => move(b.id, 1)} className="rounded-lg bg-white/10 px-2 text-xs">↓</button></div></div>)}</div></aside><section className="grid min-h-0 grid-cols-1 overflow-hidden xl:grid-cols-[1fr_420px]"><div className="min-h-0 overflow-auto bg-[radial-gradient(circle_at_10%_0%,rgba(250,204,21,.12),transparent_24rem)] p-3 lg:p-6"><div className={`mx-auto overflow-hidden rounded-[2rem] bg-white text-black shadow-[0_24px_80px_rgba(0,0,0,.55)] ${pageWidth}`}><style dangerouslySetInnerHTML={{ __html: css().replace('<style>', '').replace('</style>', '') }} /><div data-preset={state.visualPreset} className="page-wrap" dangerouslySetInnerHTML={{ __html: state.blocks.map((b) => renderBlock(b, true)).join('') }} onClick={(e) => { const target = (e.target as HTMLElement).closest('[data-id]') as HTMLElement | null; if (target?.dataset.id) setSelectedId(target.dataset.id); }} /></div></div><aside className="min-h-0 overflow-auto border-l border-yellow-300/15 bg-[#0d0906] p-4"><div className="mb-4 rounded-[1.5rem] border border-yellow-300/15 bg-white/[0.045] p-4"><div className="flex justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-yellow-300">Borrador local</p><h3 className="mt-1 text-sm font-black">{dirty ? 'Cambios pendientes' : 'Sincronizado'}</h3></div><span className={`rounded-full px-2 py-1 text-[10px] font-black ${dirty ? 'bg-orange-400/15 text-orange-200' : 'bg-emerald-400/15 text-emerald-200'}`}>{dirty ? 'No publicado' : 'OK'}</span></div><p className="mt-2 text-xs text-[#9f8d74]">Autosave: {timeLabel(lastAutosave)}</p><p className="text-xs text-[#9f8d74]">Última publicación: {timeLabel(lastPublishedAt)}</p>{showRecovery && <button onClick={() => setShowRecovery(false)} className="mt-2 rounded-xl bg-yellow-400 px-3 py-2 text-[11px] font-black text-black">Borrador recuperado · OK</button>}<button onClick={clearDraft} className="mt-3 w-full rounded-2xl border border-white/10 bg-black/25 px-4 py-3 text-xs font-black text-[#f5d195]">Limpiar borrador local</button></div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-yellow-300">Inspector</p><h2 className="mt-1 text-lg font-black">{selected?.type || 'Sin selección'}</h2>{selected && <div className="mt-4 grid gap-3"><Edit label="Título" value={selected.title} onChange={(v) => patch(selected.id, { title: v })} /><Area label="Texto" value={selected.text} onChange={(v) => patch(selected.id, { text: v })} />{selected.type === 'custom' && <Area label="HTML" value={selected.html || ''} onChange={(v) => patch(selected.id, { html: safeHtml(v) })} />}<Edit label="Imagen / URL" value={selected.image || ''} onChange={(v) => patch(selected.id, { image: v })} /><Edit label="Texto botón" value={selected.buttonText || ''} onChange={(v) => patch(selected.id, { buttonText: v })} /><Edit label="URL botón" value={selected.buttonHref || ''} onChange={(v) => patch(selected.id, { buttonHref: v })} /><Color label="Fondo" value={selected.background} onChange={(v) => patch(selected.id, { background: v })} /><Color label="Texto" value={selected.textColor} onChange={(v) => patch(selected.id, { textColor: v })} /><Color label="Accent" value={selected.accent} onChange={(v) => patch(selected.id, { accent: v })} /><button onClick={() => remove(selected.id)} className="rounded-2xl bg-red-400/10 px-4 py-3 text-sm font-black text-red-300">Borrar bloque</button></div>}<div className="mt-6 grid gap-3 rounded-[1.5rem] border border-yellow-300/15 bg-white/[0.045] p-4"><p className="text-[10px] font-black uppercase tracking-[0.22em] text-yellow-300">Carga JSON / HTML</p><textarea value={jsonText} onChange={(e) => { setJsonText(e.target.value); setJsonState('idle'); setDirty(true); }} rows={8} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-2.5 font-mono text-xs text-white outline-none" /><div className="grid grid-cols-2 gap-2"><button onClick={() => validateJson()} className="rounded-2xl border border-yellow-300/20 bg-white/10 px-4 py-3 text-xs font-black">Validar JSON</button><button onClick={applyJson} className="rounded-2xl bg-yellow-400 px-4 py-3 text-xs font-black text-black">Aplicar JSON</button><button onClick={() => { setJsonText(exampleJson); setDirty(true); }} className="rounded-2xl border border-white/10 bg-white/10 px-4 py-3 text-xs font-black">Ejemplo</button><button onClick={() => navigator.clipboard.writeText(jsonText).then(() => setStatus('JSON copiado.'))} className="rounded-2xl border border-white/10 bg-white/10 px-4 py-3 text-xs font-black">Copiar JSON</button></div><p className={`rounded-2xl border px-3 py-2 text-xs ${jsonState === 'invalid' ? 'border-red-400/30 bg-red-400/10 text-red-200' : jsonState === 'applied' ? 'border-emerald-400/30 bg-emerald-400/10 text-emerald-200' : jsonState === 'valid' ? 'border-yellow-300/30 bg-yellow-300/10 text-yellow-100' : 'border-white/10 bg-black/20 text-[#9f8d74]'}`}>{jsonMessage}</p></div><div className="mt-6 grid gap-3 rounded-[1.5rem] border border-yellow-300/15 bg-white/[0.045] p-4"><label className="grid gap-1 text-xs font-black uppercase tracking-widest text-[#9f8d74]">Vence en horas<input type="number" value={expiresHours} onChange={(e) => { setExpiresHours(Number(e.target.value) || 24); setDirty(true); }} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-2.5 text-sm text-white outline-none" /></label><button onClick={publish} disabled={saving} className="rounded-2xl bg-yellow-400 px-4 py-3 text-sm font-black text-black disabled:opacity-60">{saving ? 'Guardando…' : dirty ? 'Guardar BD + publicar cambios' : 'Publicado / guardar otra vez'}</button><button onClick={() => navigator.clipboard.writeText(html).then(() => setStatus('HTML copiado.'))} className="rounded-2xl border border-white/10 bg-white/10 px-4 py-3 text-sm font-black">Copiar HTML</button>{publicUrl && <a className="break-all text-sm text-yellow-300 underline" href={publicUrl} target="_blank" rel="noreferrer">{publicUrl}</a>}{status && <p className="text-sm text-zinc-300">{status}</p>}</div>{docs.length > 0 && <div className="mt-6 grid gap-2 rounded-[1.5rem] border border-yellow-300/15 bg-white/[0.045] p-4"><p className="text-[10px] font-black uppercase tracking-[0.22em] text-yellow-300">Páginas recientes</p>{docs.slice(0, 6).map((doc) => <a key={doc.token} href={`/w/${doc.token}`} target="_blank" rel="noreferrer" className="rounded-2xl border border-white/10 bg-black/25 p-3 text-xs hover:border-yellow-300/40"><b className="block text-white">{doc.title}</b><span className="text-[#9f8d74]">/{doc.token} · {doc.status}</span></a>)}</div>}</aside></section></main>;
}
function Edit({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { return <label className="grid gap-1 text-xs font-black uppercase tracking-widest text-[#9f8d74]">{label}<input value={value} onChange={(e) => onChange(e.target.value)} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-2.5 text-sm text-white outline-none" /></label>; }
function Area({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { return <label className="grid gap-1 text-xs font-black uppercase tracking-widest text-[#9f8d74]">{label}<textarea value={value} onChange={(e) => onChange(e.target.value)} rows={5} className="rounded-2xl border border-yellow-300/15 bg-black/40 px-3 py-2.5 text-xs text-white outline-none" /></label>; }
function Color({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { return <label className="grid gap-1 text-xs font-black uppercase tracking-widest text-[#9f8d74]">{label}<input type="color" value={value} onChange={(e) => onChange(e.target.value)} className="h-11 rounded-2xl border border-yellow-300/15 bg-black/40 p-1" /></label>; }
