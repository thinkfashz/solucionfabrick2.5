export const dynamic = 'force-dynamic';
export const metadata = { title: 'Compra confirmada — Soluciones Fabrick' };

import AceptadaClient from './AceptadaClient';
export default function AceptadaPage() {
  return <AceptadaClient />;
}
