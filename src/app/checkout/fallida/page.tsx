export const dynamic = 'force-dynamic';
export const metadata = { title: 'Compra no completada — Soluciones Fabrick' };

import FallidaClient from './FallidaClient';
export default function FallidaPage() {
  return <FallidaClient />;
}
